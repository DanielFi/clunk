Map = {} => {};
Set = {Key, Value, M} => {Key => Value, M};
Get = {Key, M} => M(Key);

MyMap = Map{};
MyMap1 = Set{name, john, MyMap};
MyMap2 = Set{cat, whiskers, MyMap1};
Name = Get{name, MyMap2};
MyMap3 = Set{name, walter, MyMap2};
Name1 = Get{name, MyMap3}.
